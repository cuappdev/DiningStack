use_frameworks!

def shared_pods
    pod 'Alamofire'
    pod 'SwiftyJSON'
end

target :'DiningStack' do
    platform:ios, '9.0'
    shared_pods
end

target :'DiningStack WatchKit' do
    platform :watchos, '2.0'
    shared_pods
end
